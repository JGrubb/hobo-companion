<h2><%= @song.title %></h2>

<% if @versions.count > 1 %>
  <p>First played: <%= link_to(@versions.first.show.date.strftime('%B %d, %Y'), @versions.first.show) %></p>
  <p>Last played: <%= link_to(@versions.last.show.date.strftime('%B %d, %Y'), @versions.last.show) %></p>
  <p>Times played: <strong><%= @versions.count %></strong></p>
<% end %>

<div id="shows-played" class="span5">
  <h4>Shows played</h4>
  <% @versions.each do |v| %>
    <div><%= v.show.date %> - <%= link_to v.venue_name, v.show %></div>
  <% end %>
</div>

<div id="lyrics" class="span6 well">
  <h4>Lyrics/Notes</h4>
  <% if @song.notes %>
    <%= simple_format @song.notes %>
  <% else %>
    <p>There are none!  <%= link_to "Please add some!", edit_song_path(@song), :class => "btn btn-mini" %></p>
  <% end %>
  <% if current_user %>
    <%= link_to "Edit", edit_song_path(@song), :class => "btn btn-small btn-success" %>
  <% end %>
</div>