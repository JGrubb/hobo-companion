$("#untag_show_<%= @show.id %>").remove();
$("td#button_<%= @show.id %>").append("<%= j link_to("I was there", tag_show_path(@show.id), :method => :post, :remote => true, :class => "btn btn-mini btn-primary", :id => "tag_show_#{@show.id}") %>");
